@using DhruvEnterprises.Core;
@using DhruvEnterprises.Web.Models.Others
@model DhruvEnterprises.Dto.OtpVerficationDto

@{
    ViewBag.Title = "Setting : Api Token";
    Layout = "~/Views/Shared/_Layout.cshtml";

}
<div id="modal-add-edit-Otprole" class="cmxform">
    @using (Html.BeginForm(FormMethod.Post))
    {
        @Html.HiddenFor(model => model.UserId)
        @Html.HiddenFor(model => model.Url)
        @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Verfiy" })
        <div id="validation-summary"></div>
        <div class="modal-body form-horizontal">

            <div class="form-group required">
                @Html.LabelFor(m => m.OTP, new { @class = "col-lg-3 col-sm-3 control-label" })
                <div class="col-lg-9 col-sm-9">
                    @Html.TextBoxFor(m => m.OTP, htmlAttributes: new { @class = "form-control ", @placeholder = "OTP" })
                </div>
            </div>
            @*<div class="form-group">
                    @Html.LabelFor(m => m.IsActive, new { @class = "col-lg-3 col-sm-3 control-label" })
                    <div class="col-lg-9 col-sm-9 checkbox popup-check">
                        @Html.CheckBoxFor(m => m.IsActive)
                    </div>
                </div>*@

        </div>
        @Html.Partial("_ModalFooter", new ModalFooter { })
    }
</div>
